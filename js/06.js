// Objeto Math en JS

let resultado;
resultado = Math.PI; // Retorna el valor Pi.
resultado = Math.round(2.2); // Redondea el numero.
resultado = Math.ceil(2.4); // Retorna el valor de la función techo, redondeado haca arriba.
resultado = Math.ceil(2.9); // Retona el valor de la funcón piso, redondeando hacia abajo.
resultado = Math.sqrt(4); // Raź cadrada
resultado = Math.abs(-5); // Retorna el valor absoluto
resultado = Math.min(-5, 4, 8); // Retorna el valor minimo
resultado = Math.max(-5, 4, 8); // Retorna el valor maximo
resultado = Math.random(); // Genera un numero aleatorio
resultado = Math.floor(Math.random() * 30) // Genera valores enteros aleatorios
console.log(resultado);